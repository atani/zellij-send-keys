// Coaching Staff Layout (8-person team)
// Coach -> Reviewer/Tactician/QA Lead/Coordinator/Tester/Workers

layout {
    default_tab_template {
        pane size=1 borderless=true {
            plugin location="tab-bar"
        }
        children
        pane size=1 borderless=true {
            plugin location="status-bar"
        }
    }

    // Coach: Lead specialist, integration, and final decision maker
    tab name="Coach" focus=true {
        pane name="coach" {
            command "claude"
        }
    }

    // Reviewer: Code review specialist and design guardrails
    tab name="Reviewer" {
        pane name="reviewer" {
            command "claude"
        }
    }

    // Strategy: Architecture and quality strategy
    tab name="Strategy" {
        pane split_direction="vertical" {
            // Tactician: Architecture specialist, patterns, and system design
            pane name="tactician" {
                command "claude"
            }
            // QA Lead: Quality specialist, test strategy, and release criteria
            pane name="qa-lead" {
                command "claude"
            }
        }
    }

    // Operations: Coordination and test execution
    tab name="Operations" {
        pane split_direction="vertical" {
            // Coordinator: Coordination specialist, blockers, and cross-team sync
            pane name="coordinator" {
                command "claude"
            }
            // Tester: Testing specialist for coverage and edge cases
            pane name="tester" {
                command "claude"
            }
        }
    }

    // Workers: Implementation
    tab name="Workers" {
        pane split_direction="vertical" {
            // Worker A: Implementation specialist
            pane name="worker-a" {
                command "claude"
            }
            // Worker B: Implementation specialist
            pane name="worker-b" {
                command "claude"
            }
        }
    }

    // VAR: Monitoring plugin
    tab name="VAR" {
        pane {
            plugin location="file:~/.config/zellij/plugins/zellij-send-keys.wasm"
        }
    }
}
